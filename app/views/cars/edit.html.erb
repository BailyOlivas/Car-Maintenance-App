<% content_for :title, "Editing car" %>

<h1>Editing car</h1>

<%= render "form", car: @car %>

<br>

<div>
  <%= link_to "Show this car", user_car_path(current_user, @car) %> |
  <%= link_to "Back to cars", user_cars_path(current_user) %>
</div>
